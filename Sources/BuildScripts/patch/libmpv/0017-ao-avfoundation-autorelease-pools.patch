diff --git a/audio/out/ao_avfoundation.m b/audio/out/ao_avfoundation.m
index 3697f50..777902c 100644
--- a/audio/out/ao_avfoundation.m
+++ b/audio/out/ao_avfoundation.m
@@ -402,8 +402,13 @@ static void feed(struct ao *ao)
     if (sample_buffer) CFRelease(sample_buffer);
 }

+// Every entry point mpv calls on its own threads wears a pool: those threads never pop
+// an autorelease pool of their own, so anything AVFoundation autoreleases in here would
+// otherwise sit in the thread-exit pool — a leak all session and a use-after-free bomb
+// at mpv_terminate_destroy (the yadif_videotoolbox lesson, 2026-08-03).
 static void start(struct ao *ao)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;

     // feed/probe and notification-driven restarts also live on this queue. Reset there so a
@@ -425,10 +430,12 @@ static void start(struct ao *ao)
     [p->renderer requestMediaDataWhenReadyOnQueue:p->queue usingBlock:^{
         feed(ao);
     }];
+    }
 }

 static void stop(struct ao *ao)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;

     dispatch_sync(p->queue, ^{
@@ -436,10 +443,12 @@ static void stop(struct ao *ao)
         [p->renderer flush];
         [p->synchronizer setRate:0];
     });
+    }
 }

 static bool set_pause(struct ao *ao, bool paused)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;

     if (paused) {
@@ -449,10 +458,12 @@ static bool set_pause(struct ao *ao, bool paused)
     }

     return true;
+    }
 }

 static int control(struct ao *ao, enum aocontrol cmd, void *arg)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;

     switch (cmd) {
@@ -471,6 +482,7 @@ static int control(struct ao *ao, enum aocontrol cmd, void *arg)
     default:
         return CONTROL_UNKNOWN;
     }
+    }
 }

 @implementation AVObserver
@@ -512,6 +524,7 @@ - (void)handleRestartNotification:(NSNotification*)notification {

 static int init(struct ao *ao)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;
     AudioChannelLayout *layout = NULL;

@@ -658,10 +671,12 @@ static int init(struct ao *ao)
 #endif

     return CONTROL_ERROR;
+    }
 }

 static void uninit(struct ao *ao)
 {
+    @autoreleasepool {
     struct priv *p = ao->priv;

     // Teardown emits the very notifications the observer restarts on — the flush below,
@@ -696,6 +711,7 @@ static void uninit(struct ao *ao)
 #endif

     dispatch_release(p->queue);
+    }
 }

 #define OPT_BASE_STRUCT struct priv
